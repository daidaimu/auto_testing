```
set和eval命令 
wget指令
expr指令： `expr 2 + 2` 两边要保持空格,要用``括起来
bc管道命令，用于计算
tr
wc
exec
RANDOM
seq 9 -1  
cut
hostname   显示
du
df
find
cp 
rmdir 用于删除空目录
mkdir -p 创建多个子目录
cat 
more 下翻页
less 上下翻动文本
tail -f / -n 100 one.txt
head -f / -n 5 two.txt
kill  -1进程挂起 -2中断 -3结束 -9无条件终止
mount
umount
df -h   查看挂着设备使用的内存情况
du -ch 列出文件所占内存   du -sh 查看整个目录所占内存
```



```
grep:   语法： grep -v 3  匹配3以外的其他内容





```







```
vim 

set mouse=a
set showmatch 显示匹配的括号
%s/A/B/g    将字符A替换成B
```



```
gzip：     压缩数据
gunzip：   解压数据
```

